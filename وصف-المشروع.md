# 3D DZ — الوصف التجاري والتقني للمشروع

> **السوق الرقمي الجزائري لبيع ملفات الطباعة ثلاثية الأبعاد**
> نسخة: 1.0 — تاريخ: 2026

---

## الجزء الأول: الوصف التجاري (Commercial Description)

### 1.1 ملخص تنفيذي
**3D DZ** هو منصة رقمية جزائرية متخصصة في **بيع ملفات الطباعة ثلاثية الأبعاد** (STL / OBJ / 3MF وغيرها)، بنموذج تشغيلي بمالك واحد (Single-Owner) قريب من روح منصات مثل Cults3D لكن موجه للسوق الجزائرية. المنصة تتيح للزائر تصفح الكتالوج، البحث عن النماذج، والاطلاع على تفاصيل كل منتج، ثم تقديم طلب شراء تتم معالجته يدويًا عبر لوحة تحكم خاصة بالمالك.

المنصة تهدف إلى سد فجوة في السوق الجزائري من خلال توفير مكتبة نماذج طباعة ثلاثية الأبعاد بأسعار محلية (دينار جزائري)، مع دعم كامل للغة العربية والفرنسية والإنجليزية، وتغطية الولايات الـ 69 في التقسيم الإداري الجديد.

### 1.2 نموذج العمل
- **المنتج:** ملفات رقمية جاهزة للطباعة ثلاثية الأبعاد (قابلة للتحميل بعد الشراء).
- **طريقة البيع:** لا توجد بوابة دفع أوتوماتيكية. يقدّم الزبون **طلبًا** يحتوي (الاسم، رقم الهاتف، البريد الإلكتروني، الولاية).
- **المعالجة:** يصل الطلب إلى لوحة تحكم المالك، الذي يتواصل مع الزبون (هاتفًا/بريدًا) لترتيب الدفع والتسليم **خارج المنصة**، ثم يحدّث حالة الطلب يدويًا.
- **التحصيل والتسليم:** يتم خارج المنصة بالكامل (تحويل بنكي، فوري، استلام، إلخ) وفق ما يتفق عليه المالك والزبون.

### 1.3 الجمهور المستهدف
| الفئة | الوصف | القيمة المقدمة |
|---|---|---|
| **مصممو/هواة الطباعة** | من يمتلكون طابعة ثلاثية الأبعاد | مكتبة نماذج جاهزة باللغة العربية |
| **المهندسون والتقنيون** | ميكانيكا، أجزاء، نماذج تعليمية | ملفات دقيقة قابلة للطباعة |
| **الأشخاص والمبدعون** | ديكور، هدايا، ألعاب، كوزبلاي | نماذج فنية وزخرفية |
| **القائمون على مشاريع تعليمية/مدرسية** | مجسمات تعليمية | محتوى تعليمي ثلاثي الأبعاد |

### 1.4 الميزات الرئيسية (من منظور تجاري)

**للزبون/الزائر:**
- تصفح الكتالوج حسب الفئات والمجموعات.
- بحث متقدم مع فلاتر (السعر، الفئة، التقييم، الترتيب).
- صفحة تفاصيل منتج تشمل معاينة 3D، الصور، الترخيص، والتقييمات.
- حساب مستخدم (تسجيل، دخول، ملف شخصي، استعادة كلمة المرور).
- سلة تسوق ومفضلة.
- تقديم طلب ومتابعة حالته (قيد الانتظار، مؤكد، مرفوض، منتهي).
- قراءة تقييمات معتمدة وكتابة تقييم بعد الشراء.
- واجهة بثلاث لغات (عربي/فرنسي/إنجليزي) مع دعم RTL للعربية.

**للمالك/الأدمين:**
- لوحة تحكم شاملة: إحصائيات، منتجات، فئات، مجموعات، بانرات، طلبات، زبائن، تقييمات، إشعارات.
- إدارة كاملة للمحتوى (رفع منتجات وصور وملفات 3D عبر ImageKit).
- معالجة الطلبات يدويًا مع تغيير الحالة وملاحظات داخلية (سجل زمني).
- إدارة الأسعار والعروض/الخصومات بمرونة.
- مراجعة التقييمات قبل نشرها، وحظر/تفعيل الحسابات.

### 1.5 المزايا التنافسية
- **هوية محلية:** أسعار بالدينار الجزائري، تغطية 69 ولاية، ثنائية اللغة (عربي/فرنسي) الأساسية.
- **نموذج مرن:** بدون تعقيدات بوابة دفع — معالجة يدوية تتيح تفاهمًا مباشرًا مع الزبون.
- **تجربة عصرية:** تصميم "Kinetic Dimension" (زجاجي داكن عالي الطاقة) مع معاينات 3D حقيقية.
- **تحكم كامل للمالك الواحد:** قرارات تسعير ومحتوى سريعة دون وسطاء.

### 1.6 نموذج الإيرادات
- **بيع مباشر:** إيرادات من كل ملف يُباع (سعر محدد بالدينار الجزائري).
- **استراتيجية تسعير مرنة:** إمكانية تطبيق عروض وخصومات لتحفيز المبيعات.
- **توسعة مستقبلية:** إمكانية إضافة بوابة دفع أوتوماتيكية، أو تحميل ملفات إضافية مدفوعة.

---

## الجزء الثاني: الوصف التقني (Technical Description)

### 2.1 المعمارية العامة (Architecture)
نظام **full-stack** من جزئين منفصلين يتواصلان عبر REST API:

```
┌─────────────────────┐        REST/JSON        ┌──────────────────────────┐
│  Frontend (React)   │  ─────────────────────► │  Backend (.NET 10 API)  │
│  Vite + TS + Tailwind│ ◄───────────────────── │  Clean Architecture      │
└─────────────────────┘        JWT + Axios       └────────────┬─────────────┘
                                                              │ MongoDB Atlas
                                                              ▼
                                                       ┌──────────────┐
                                                       │  MongoDB     │
                                                       │  + ImageKit  │
                                                       └──────────────┘
```

### 2.2 التقنيات المعتمدة (Tech Stack)

| الطبقة | التقنية |
|---|---|
| **Backend** | ASP.NET Core (.NET 10), Clean Architecture (Domain/Application/Infrastructure/Api) |
| **قاعدة البيانات** | MongoDB Atlas (سحابية) |
| **المصادقة** | JWT + BCrypt (دوران: Admin / Customer) |
| **تخزين الملفات** | ImageKit (صور + ملفات 3D) |
| **الترجمة الآلية** | LibreTranslate (للمحتوى الديناميكي) |
| **Frontend** | React 19 + Vite 8 + TypeScript |
| **التنسيق** | Tailwind CSS v4 (هوية "Kinetic Dimension") |
| **إدارة الحالة** | Zustand (auth + cart) |
| **التوجيه** | React Router v7 |
| **التحويل الدولي** | i18next (عربي/فرنسي/إنجليزي) |
| **معاينة 3D** | @google/model-viewer |

### 2.3 بنية مجلدات Backend (Clean Architecture)
```
backend/src/
  ThreeDDz.Domain/           # الكيانات، الـ Enums، الـ Models
    Models/                  # Product, Category, Collection, Banner, Order, Review, User, Wilaya, FavoriteAndCart, LocalizedString
    Enums/Enums.cs
  ThreeDDz.Application/      # الواجهات (Interfaces) — Repositories & Services
    Interfaces/
  ThreeDDz.Infrastructure/   # التنفيذ الفعلي
    Repositories/            # MongoRepository, ProductRepository, ...
    Services/                # AuthService, ProductService, OrderService, ReviewService, CartFavoriteService, AnalyticsService, ImageKitService, JwtService, ...
  ThreeDDz.Api/              # Controllers (thin), Program.cs, Seed, Validators, Middleware
```

### 2.4 بنية مجلدات Frontend
```
frontend/src/
  core/                      # api/client.ts, auth/store.ts, store/cart.ts, i18n/, theme/
  data/                      # repos/ (productRepo, categoryRepo, orderRepo, adminRepo), types/
  presentation/
    features/                # home, catalog, product, cart, checkout, auth, account, admin
    shared/                  # layout, ProductCard, NotFound
  App.tsx, main.tsx, index.css
```

### 2.5 نماذج البيانات (MongoDB Collections)
`users`، `products`، `categories`، `collections`، `banners`، `reviews`، `orders`، `carts`، `favorites`، `wilayas` (69 ولاية).

**ملاحظة لغوية:** الحقول الديناميكية (اسم المنتج، الوصف، اسم الفئة...) تُخزَّن ككائن متعدد اللغات:
```json
{ "name": { "ar": "...", "fr": "...", "en": "..." } }
```

### 2.6 المصادقة والأمان
- JWT Bearer (بدون Refresh Tokens) مع دوري Admin وCustomer.
- تشفير كلمات المرور بـ BCrypt.
- Admin واحد مُنشأ مسبقًا (لا يمكن التسجيل الذاتي كأدمن).
- حظر/تفعيل الحسابات (`isBanned`) يمنع دخول الزبون المحظور.
- التحقق من الصلاحيات على كل نقطة نهاية (Admin-only للمهام الإدارية).
- حماية الخصوصية: لا تُكشف بيانات كلمة المرور، رسائل خطأ عامة لأسباب أمنية.

### 2.7 واجهة الـ API (نقاط رئيسية)
| المجال | أمثلة النقاط |
|---|---|
| المصادقة | `POST /api/auth/login`، `register`، `me`، `profile`، `forgot-password`، `reset-password` |
| المنتجات | `GET/POST/PUT/DELETE /api/products`، `GET /api/products/{slug}`، `PATCH /{id}/featured` |
| الفئات/المجموعات/البانرات | `GET/POST/PUT/DELETE` لكل كيان |
| السلة/الطلبات | `POST /api/cart/add`، `POST /api/orders/place`، `GET /api/orders/my` |
| التقييمات | `POST /api/reviews`، `GET /api/reviews/product/{id}`، `GET .../can-review/{id}` |
| لوحة الأدمين | analytics، orders (+status/notes)، customers (+ban)، reviews (approve/reject) |
| رفع الملفات | `POST /api/upload` (ImageKit، Admin-only) |

### 2.8 نموذج العمل التقني (منطق الطلبات)
- الطلب ينشأ بحالة `Pending` ويرتبط بـ `customerId`.
- السلة **لا تُفرَّغ عند فشل الطلب**، وتُفرَّغ فقط بعد نجاح إنشائه.
- حالات الطلب: `Pending / Confirmed / Rejected / Completed` مع سجل زمني (Audit trail).
- الملاحظات الداخلية سجل إلحاقي (append-only) بزمن ومُعرف الأدمن.
- حذف المنتج **Soft Delete** (`isDeleted`) للحفاظ على سجل الطلبات القديمة.
- التقييم: يُسمح فقط لمن اشترى المنتج (طلب بحالة `Completed`)، مع إمكانية التعديل بدل التكرار.
- حذف فئة بها منتجات ممنوع (رسالة توجيهية).

### 2.9 الترجمة و RTL
- 3 لغات: العربية (ar) والفرنسية (fr) والإنجليزية (en) عبر i18next.
- المحتوى الديناميكي (منتجات/فئات) يُخزَّن متعدد اللغات، ويُملأ تلقائيًا بالترجمة الآلية (LibreTranslate) عند إنشاء المحتوى، مع إمكانية التعديل اليدوي من الأدمين.
- دعم RTL عند اختيار العربية.

### 2.10 البيانات الأولية (Seed Data)
- حساب Admin افتراضي:
  - البريد: `admin@3ddz.dz`
  - كلمة المرور: `Admin123!` (يجب تغييرها بعد أول دخول)
- 69 ولاية (التقسيم 2026: 58 + 11 جديدة).
- 10 فئات، 3 مجموعات، 18 منتجًا، 5 زبائن، 6 طلبات بحالات مختلفة، 4 تقييمات معتمدة، وبانر واحد.

---

## الجزء الثالث: الجاهزية وخطوات التشغيل

### 3.1 الحالة الحالية
- Backend: مكتمل (~98%) — مبني بنجاح (0 أخطاء).
- Frontend: مكتمل (~98%) — type-check و build ناجحان (0 أخطاء).
- مجلد `backend/tests/` فارغ → **يُنصح بإضافة اختبارات** (وحدة/تكامل/E2E) قبل الإطلاق.

### 3.2 خطوات التشغيل
```bash
# Backend
cd backend
cp .env.example src/ThreeDDz.Api/.env   # املأ MongoDB / ImageKit / JWT secret
dotnet run --project src/ThreeDDz.Api/ThreeDDz.Api.csproj

# Frontend
cd frontend
npm install
npm run dev   # http://localhost:5173
```

### 3.3 قيود ونواقص معلّقة (توسعات مستقبلية)
- **الدفع الإلكتروني:** لا يوجد — معالجة يدوية (قرار مقصود).
- **البريد الإلكتروني:** خدمة placeholder (تسجيل في اللوق فقط) — لم يُربط مزود SMTP بعد.
- **فحص عمر JWT في localStorage:** يُنصح لاحقًا بالانتقال إلى HttpOnly cookie.
- **الملفات الحقيقية للطباعة:** تُرفع يدويًا عبر لوحة الأدمين (لا توجد ملفات في الـ seed).
- **اختبارات:** غير موجودة بعد — عنصر ضروري قبل الإنتاج.
- **تحسينات RTL:** أساسها جاهز ويحتاج لمسة ناعمة إضافية.

---

## خاتمة
**3D DZ** مشروع متكامل وجاهز للتشغيل محليًا، يجمع بين نموذج عمل تجاري واضح (بيع ملفات الطباعة ثلاثية الأبعاد بمعالجة يدوية) وتنفيذ تقني احترافي (Clean Architecture + React + MongoDB + إدارة محتوى كاملة). المنصة مؤهلة للانطلاق في السوق الجزائرية، مع مسار واضح للتوسعة المستقبلية (دفع إلكتروني، مزود بريد، اختبارات، ملفات حقيقية).
