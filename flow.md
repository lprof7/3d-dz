# وصف التدفقات التفصيلية (Detailed Flows) لكل User Story — مشروع 3D DZ

> لكل User Story: الهدف، الشروط المسبقة، التدفق الرئيسي خطوة بخطوة، التدفقات البديلة/الأخطاء، والنتيجة النهائية.

---

## 🔵 Epic A — تصفح المنتجات

### US-A1 — الصفحة الرئيسية
**الشروط المسبقة:** لا يوجد (متاحة للجميع).
**التدفق الرئيسي:**
1. المستخدم يفتح الرابط الرئيسي للموقع.
2. النظام يجلب: قائمة المنتجات المميزة (Featured)، البانرات النشطة، الفئات الرئيسية، آخر المنتجات المضافة.
3. تُعرض الصفحة: Hero Banner، شريط الفئات، Carousel المنتجات المميزة، قسم "الأحدث".
4. المستخدم يمكنه الضغط على أي عنصر (منتج/فئة/بانر) للانتقال لصفحته.
**التدفقات البديلة:**
- إذا لم توجد منتجات مميزة بعد → يُعرض قسم "الأحدث" فقط، ويُخفى قسم Featured.
- فشل تحميل البيانات (خطأ شبكة/سيرفر) → عرض رسالة خطأ ودّية + زر "إعادة المحاولة".
**النتيجة:** المستخدم يرى نظرة عامة على الموقع ويمكنه بدء التصفح.

---

### US-A2 — تصفح حسب الفئات والمجموعات
**الشروط المسبقة:** توجد فئة/مجموعة واحدة على الأقل.
**التدفق الرئيسي:**
1. المستخدم يضغط على فئة من القائمة الرئيسية أو من صفحة "كل الفئات".
2. النظام يوجّهه إلى صفحة `/category/{slug}` أو `/collection/{slug}`.
3. يُجلب اسم الفئة (باللغة الحالية) + قائمة منتجاتها (Paginated، مثلاً 20 منتج بالصفحة).
4. تُعرض المنتجات في شبكة (Grid) مع صورة مصغرة، الاسم، السعر، متوسط التقييم.
5. المستخدم يمكنه التنقل بين الصفحات (Pagination) أو تطبيق فلاتر (انظر US-A4).
**التدفقات البديلة:**
- الفئة لا تحتوي منتجات بعد → عرض حالة فارغة (Empty State) مع رسالة "لا توجد منتجات حاليًا في هذه الفئة" ورسمة 3D.
- Slug غير موجود (رابط خاطئ) → صفحة 404 مخصصة بنفس الهوية البصرية.
**النتيجة:** المستخدم يشاهد كل منتجات الفئة/المجموعة المطلوبة.

---

### US-A3 — صفحة تفاصيل المنتج
**الشروط المسبقة:** المنتج موجود ومنشور (isPublished = true).
**التدفق الرئيسي:**
1. المستخدم يضغط على منتج من أي شبكة عرض.
2. النظام يوجّهه إلى `/product/{slug}`.
3. يُجلب: معرض الصور/المعاينات، الاسم والوصف (باللغة الحالية)، السعر، معلومات الترخيص/الاستخدام، متوسط التقييم وعدد المراجعات، حالة التوفر.
4. تُعرض أزرار: "إضافة للسلة"، "إضافة للمفضلة" (تتطلب تسجيل الدخول)، تبويبات (الوصف الكامل / الترخيص / التقييمات).
5. في الأسفل: قسم "منتجات ذات صلة" (نفس الفئة).
**التدفقات البديلة:**
- المستخدم غير مسجل ويضغط "إضافة للمفضلة" → توجيه لصفحة تسجيل الدخول مع رسالة "سجّل دخولك لإضافة هذا المنتج لمفضلتك".
- المنتج غير منشور أو محذوف → صفحة 404.
- لا توجد تقييمات بعد → تبويب التقييمات يعرض "كن أول من يقيّم هذا المنتج" (يظهر فقط لمن اشتراه).
**النتيجة:** المستخدم يحصل على كل المعلومات اللازمة لاتخاذ قرار الطلب.

---

### US-A4 — البحث والفلترة
**الشروط المسبقة:** لا يوجد.
**التدفق الرئيسي:**
1. المستخدم يكتب كلمة في شريط البحث (متاح في الـ Header بكل الصفحات) ويضغط Enter/أيقونة البحث.
2. النظام يبحث في حقول: اسم المنتج + الوصف (بكل اللغات المخزّنة) عبر MongoDB text index.
3. يُعرض عدد النتائج + الكلمة المستخدمة + شبكة النتائج بنفس تصميم صفحة الفئة.
4. المستخدم يمكنه تطبيق فلاتر إضافية من الشريط الجانبي: نطاق السعر (Slider)، الفئة، التقييم (4 نجوم فأكثر...)، والترتيب (الأحدث/الأقل سعرًا/الأعلى سعرًا/الأعلى تقييمًا).
5. كل تغيير فلتر يُحدّث النتائج فورًا (مع تحديث الـ URL query params حتى يمكن مشاركة الرابط).
**التدفقات البديلة:**
- لا نتائج مطابقة → رسالة "لم نجد نتائج لبحثك" + اقتراح بمسح الفلاتر أو تصفح الفئات.
- كلمة بحث فارغة → لا يتم تنفيذ البحث، يبقى المستخدم في مكانه.
**النتيجة:** المستخدم يصل لقائمة منتجات مطابقة لمعايير بحثه.

---

## 🟢 Epic B — حساب المستخدم

### US-B1 — إنشاء حساب
**الشروط المسبقة:** الزائر غير مسجل دخول.
**التدفق الرئيسي:**
1. الزائر يضغط "تسجيل" من الـ Header.
2. يملأ النموذج: الاسم الكامل، البريد الإلكتروني، رقم الهاتف، كلمة المرور، تأكيد كلمة المرور.
3. يضغط "إنشاء حساب" → النظام يتحقق من صحة البيانات (Validation) محليًا (Frontend) ثم في الـ API.
4. الـ API يتحقق أن البريد غير مستخدم مسبقًا، يشفّر كلمة المرور (bcrypt/argon2)، ينشئ المستخدم بدور `Customer`.
5. يُعاد JWT Token للمستخدم، يُسجَّل دخوله تلقائيًا، ويُوجَّه للصفحة الرئيسية مع رسالة ترحيب.
**التدفقات البديلة:**
- البريد مستخدم مسبقًا → رسالة خطأ "هذا البريد مسجّل مسبقًا، سجّل الدخول بدلاً من ذلك" + رابط لصفحة الدخول.
- كلمة المرور لا تحقق الحد الأدنى (مثلاً 8 خانات) → رسالة خطأ توضيحية تحت الحقل مباشرة.
- كلمتا المرور غير متطابقتين → رسالة خطأ فورية قبل الإرسال.
**النتيجة:** حساب جديد مُنشأ، والمستخدم مسجّل دخوله تلقائيًا.

---

### US-B2 — تسجيل الدخول والخروج
**الشروط المسبقة:** يملك المستخدم حسابًا.
**التدفق الرئيسي (دخول):**
1. المستخدم يفتح صفحة تسجيل الدخول، يدخل البريد وكلمة المرور.
2. الـ API يتحقق من تطابق كلمة المرور المشفّرة، ويُصدر JWT.
3. الـ Token يُخزَّن (Frontend) بطريقة آمنة (HttpOnly cookie أو Secure storage)، ويُحدَّث الـ Auth Context/Store.
4. يُوجَّه المستخدم للصفحة التي كان يحاول الوصول لها، أو للرئيسية.
**التدفق الرئيسي (خروج):**
1. المستخدم يضغط "تسجيل الخروج" من قائمة الحساب.
2. يُمسح الـ Token من التخزين، وتُعاد حالة Auth Context إلى "غير مسجل"، ويُوجَّه للرئيسية.
**التدفقات البديلة:**
- بيانات دخول خاطئة → رسالة عامة "البريد أو كلمة المرور غير صحيحة" (بدون تحديد أيهما، لأسباب أمنية).
- الحساب محظور (Banned) من الأدمين → رسالة "هذا الحساب موقوف، تواصل مع الدعم".
- انتهاء صلاحية الـ Token أثناء التصفح → إعادة توجيه تلقائي لصفحة تسجيل الدخول مع حفظ الوجهة الأصلية.
**النتيجة:** جلسة مصادقة صحيحة (JWT) أو إنهاؤها.

---

### US-B3 — تعديل الملف الشخصي
**الشروط المسبقة:** المستخدم مسجّل دخوله.
**التدفق الرئيسي:**
1. المستخدم يفتح "حسابي" → "الملف الشخصي".
2. تُعرض بياناته الحالية (الاسم، الهاتف، الولاية، البريد [غير قابل للتعديل عادة]).
3. يعدّل الحقول المطلوبة ويضغط "حفظ".
4. الـ API يتحقق من صحة البيانات ويحدّثها في قاعدة البيانات.
5. رسالة نجاح "تم تحديث بياناتك".
**التدفقات البديلة:**
- رقم هاتف بصيغة غير صحيحة → رسالة خطأ توضح الصيغة المطلوبة.
- تغيير كلمة المرور: يتطلب إدخال كلمة المرور الحالية أولًا كتأكيد أمني.
**النتيجة:** بيانات الحساب محدَّثة.

---

### US-B4 — استعادة كلمة المرور
**الشروط المسبقة:** المستخدم نسي كلمة مروره ويملك بريدًا مسجلًا.
**التدفق الرئيسي:**
1. من صفحة تسجيل الدخول، يضغط "نسيت كلمة المرور؟".
2. يدخل بريده الإلكتروني ويضغط "إرسال".
3. النظام يتحقق من وجود البريد، يولّد رمز/رابط إعادة تعيين صالح لمدة محدودة (مثلاً 30 دقيقة)، ويرسله بالبريد.
4. المستخدم يفتح الرابط من بريده، يُنقل لصفحة "كلمة مرور جديدة"، يدخلها مرتين، يضغط "تأكيد".
5. النظام يحدّث كلمة المرور المشفّرة، ويعرض رسالة نجاح مع رابط لتسجيل الدخول.
**التدفقات البديلة:**
- البريد غير مسجّل → لأسباب أمنية، تُعرض نفس رسالة "إذا كان البريد مسجّلاً، ستصلك رسالة" (بدون تأكيد وجوده أو عدمه).
- الرابط منتهي الصلاحية أو مُستخدم سابقًا → رسالة "الرابط غير صالح، اطلب رابطًا جديدًا".
**النتيجة:** كلمة مرور جديدة مفعّلة للحساب.

---

## 🟡 Epic C — المفضلة والسلة

### US-C1 — إضافة إلى المفضلة
**الشروط المسبقة:** المستخدم مسجّل دخوله.
**التدفق الرئيسي:**
1. المستخدم يضغط أيقونة "قلب/مفضلة" على بطاقة منتج أو في صفحة تفاصيله.
2. الـ API يضيف سجلًا (customerId + productId) في مجموعة Favorites (أو مصفوفة داخل مستند العميل).
3. الأيقونة تتحول بصريًا لحالة "مُفعّلة" فورًا (Optimistic UI).
4. المنتج يظهر لاحقًا في "حسابي → المفضلة".
**التدفقات البديلة:**
- المنتج مضاف مسبقًا للمفضلة والمستخدم يضغط مجددًا → يُزال من المفضلة (Toggle).
- غير مسجل دخول → توجيه لصفحة الدخول (كما في US-A3).
**النتيجة:** قائمة مفضلة محدَّثة للمستخدم.

---

### US-C2 — إضافة إلى السلة
**الشروط المسبقة:** المستخدم مسجّل دخوله (السلة مرتبطة بالحساب، وليست Guest cart، لأن الطلب يتطلب حسابًا لاحقًا).
**التدفق الرئيسي:**
1. المستخدم يضغط "إضافة للسلة" من صفحة المنتج (مع تحديد الكمية إن وُجدت).
2. المنتج يُضاف لسلة الجلسة الحالية (State محلي + مُزامَن مع الـ Backend لضمان استمراريتها بين الأجهزة).
3. أيقونة السلة في الـ Header تُحدّث عدّادها فورًا.
4. المستخدم يمكنه فتح صفحة السلة، تعديل الكميات، أو حذف عنصر، قبل الانتقال لتقديم الطلب.
**التدفقات البديلة:**
- المنتج موجود مسبقًا في السلة → تُزاد الكمية بدلاً من تكرار السطر.
- السلة فارغة عند محاولة فتح صفحة "تقديم الطلب" → إعادة توجيه لصفحة السلة مع رسالة تنبيه.
**النتيجة:** سلة تحتوي المنتجات المطلوبة جاهزة للطلب.

---

## 🟠 Epic D — تقديم الطلب (بدون دفع أوتوماتيكي)

### US-D1 — تقديم طلب
**الشروط المسبقة:** المستخدم مسجّل دخوله، والسلة تحتوي منتجًا واحدًا على الأقل.
**التدفق الرئيسي:**
1. المستخدم يضغط "متابعة الطلب" من صفحة السلة.
2. تُعرض صفحة الطلب: ملخص المنتجات (قراءة فقط) + نموذج يحتوي: الاسم الكامل (مُعبّأ مسبقًا من الحساب، قابل للتعديل)، رقم الهاتف، البريد الإلكتروني، الولاية (Dropdown من 69 ولاية).
3. المستخدم يراجع البيانات ويضغط "تأكيد الطلب".
4. الـ API ينشئ مستند Order جديد بحالة `Pending`، يربطه بـ customerId، ويُفرّغ السلة.
5. يُرسل إشعار بريدي للمستخدم (تأكيد استلام الطلب) وإشعار داخلي للأدمين (US-G5).
6. يُوجَّه المستخدم لصفحة "تم استلام طلبك بنجاح" مع رقم مرجعي للطلب.
**التدفقات البديلة:**
- فشل الإرسال (خطأ سيرفر) → رسالة خطأ + السلة تبقى كما هي (لا تُفرَّغ) حتى يعيد المحاولة.
- حقل الولاية أو الهاتف فارغ/غير صالح → منع الإرسال + رسالة تحت الحقل المعني.
- منتج في السلة أصبح غير متوفر/محذوف أثناء التقديم → إزالته تلقائيًا من الطلب مع تنبيه للمستخدم قبل التأكيد النهائي.
**النتيجة:** طلب جديد بحالة "قيد الانتظار" في قاعدة البيانات، مرئي فورًا في لوحة الأدمين.

---

### US-D2 — رسالة تأكيد الطلب
**الشروط المسبقة:** طلب تم تقديمه بنجاح (يتبع مباشرة US-D1).
**التدفق الرئيسي:**
1. فور إنشاء الطلب في الـ API، يُستدعى Notification Service.
2. تُبنى رسالة بريدية (بلغة العميل المفضّلة أو العربية افتراضيًا) تحتوي: رقم الطلب، قائمة المنتجات، "سيتم التواصل معك قريبًا لتأكيد الطلب والدفع والتسليم".
3. تُرسل عبر مزود البريد المُعرَّف في `.env`.
**التدفقات البديلة:**
- فشل إرسال البريد (مشكلة في المزود) → لا يُفشَل الطلب بسبب ذلك؛ يُسجَّل الخطأ في اللوق فقط، ويبقى الطلب صحيحًا في النظام (الأدمين يراه بأي حال في الداشبورد).
**النتيجة:** العميل يستلم تأكيدًا بالبريد.

---

### US-D3 — متابعة سجل الطلبات (العميل)
**الشروط المسبقة:** المستخدم مسجّل دخوله وقدّم طلبًا واحدًا على الأقل.
**التدفق الرئيسي:**
1. المستخدم يفتح "حسابي → طلباتي".
2. تُجلب كل الطلبات المرتبطة بـ customerId، مرتبة من الأحدث للأقدم.
3. تُعرض كبطاقات/جدول: رقم الطلب، التاريخ، الحالة (شارة ملوّنة)، الإجمالي.
4. الضغط على طلب يفتح تفاصيله الكاملة (المنتجات، الملاحظات إن وُجدت من الأدمين موجّهة للعميل).
**التدفقات البديلة:**
- لا توجد طلبات بعد → حالة فارغة مع زر "تصفح المنتجات".
**النتيجة:** العميل يعرف حالة كل طلباته لحظيًا.

---

### US-D4 — تواصل الأدمين لتأكيد الطلب
**الشروط المسبقة:** طلب بحالة `Pending` أو `Confirmed`.
**التدفق الرئيسي (خارج النظام، لكن مرتبط به):**
1. الأدمين يفتح تفاصيل الطلب من لوحة التحكم (US-G2).
2. يتصل/يراسل العميل يدويًا (هاتف/بريد) خارج الموقع لتأكيد التفاصيل وطريقة الدفع والتسليم.
3. الأدمين يعود للوحة التحكم ويغيّر حالة الطلب (US-G3) ويضيف ملاحظة داخلية (US-G4) توثّق ما تم الاتفاق عليه.
**التدفقات البديلة:**
- العميل لا يرد → الأدمين يضع ملاحظة داخلية ويُبقي الحالة `Pending` أو يغيّرها لاحقًا حسب سياسته.
**النتيجة:** تفاعل بشري مباشر يُوثَّق داخل النظام عبر تغييرات الحالة والملاحظات.

---

## 🟣 Epic E — التقييمات

### US-E1 — كتابة تقييم
**الشروط المسبقة:** العميل قدّم طلبًا يحتوي هذا المنتج وحالته `Completed` (لمنع التقييمات الوهمية).
**التدفق الرئيسي:**
1. من صفحة "طلباتي" أو صفحة المنتج، يظهر للعميل المؤهّل زر "أضف تقييمك".
2. يفتح نموذجًا: عدد نجوم (1-5) + تعليق نصي اختياري.
3. يضغط "إرسال" → يُنشأ مستند Review بحالة `PendingApproval`.
4. رسالة "شكرًا، سيظهر تقييمك بعد المراجعة".
**التدفقات البديلة:**
- العميل لم يشترِ المنتج أو الطلب لم يكتمل بعد → لا يظهر له زر التقييم أصلاً.
- العميل حاول تقييم نفس المنتج مرتين → يُسمح بالتعديل على تقييمه السابق بدلاً من إنشاء واحد جديد.
**النتيجة:** تقييم جديد بانتظار موافقة الأدمين.

---

### US-E2 — عرض التقييمات المعتمدة
**الشروط المسبقة:** توجد تقييمات بحالة `Approved` على المنتج.
**التدفق الرئيسي:**
1. أي زائر/عميل يفتح تبويب "التقييمات" في صفحة المنتج.
2. تُجلب فقط التقييمات بحالة `Approved`، مرتبة (الأحدث أو الأعلى تقييمًا).
3. يُعرض لكل تقييم: اسم العميل (أو مختصر)، عدد النجوم، التعليق، التاريخ.
4. يُحسب ويُعرض متوسط التقييم العام أعلى الصفحة/على بطاقة المنتج.
**التدفقات البديلة:**
- لا توجد تقييمات معتمدة بعد → "لا توجد تقييمات بعد، كن أول من يقيّم هذا المنتج" (إن كان مؤهلاً).
**النتيجة:** ثقة أكبر للزوار قبل تقديم الطلب.

---

## 🔴 Epic F — لوحة تحكم الأدمين: المنتجات والمحتوى

### US-F1 — رفع منتج جديد
**الشروط المسبقة:** الأدمين مسجّل دخوله (دور Admin).
**التدفق الرئيسي:**
1. من "المنتجات"، يضغط "إضافة منتج جديد".
2. يملأ: الاسم (بلغة واحدة على الأقل)، الوصف، السعر، الفئة، رفع الصور (عبر ImageKit)، رفع ملفات 3D (اختياري في هذه المرحلة، يمكن تركه فارغًا وإضافته لاحقًا)، تفعيل/تعطيل "منتج مميز".
3. عند الحفظ: إن لم تُملأ اللغتان الأخريان، يُشغَّل خدمة الترجمة التلقائية (Section 6 من prompt.md) لملء الاسم/الوصف بالفرنسية والإنجليزية تلقائيًا.
4. يُنشأ المنتج بحالة "منشور" أو "مسودة" حسب اختيار الأدمين، ويظهر فورًا في الكتالوج إن كان منشورًا.
**التدفقات البديلة:**
- فشل رفع صورة لـ ImageKit (مشكلة شبكة/حجم) → رسالة خطأ محددة، وإمكانية إعادة المحاولة بدون فقدان باقي بيانات النموذج.
- السعر سالب أو غير رقمي → منع الحفظ + رسالة خطأ.
**النتيجة:** منتج جديد جاهز للعرض (أو كمسودة).

---

### US-F2 — تعديل/حذف منتج
**الشروط المسبقة:** المنتج موجود.
**التدفق الرئيسي:**
1. الأدمين يفتح المنتج من قائمة المنتجات، يضغط "تعديل".
2. يعدّل أي حقل (بما فيها الترجمات يدويًا) ويحفظ → تحديث فوري.
3. للحذف: يضغط "حذف" → نافذة تأكيد ("هل أنت متأكد؟ هذا الإجراء لا يمكن التراجع عنه") → تأكيد → حذف (Soft Delete يُفضَّل، أي `isDeleted: true` بدلاً من حذف فعلي، للحفاظ على سجل الطلبات القديمة المرتبطة به).
**التدفقات البديلة:**
- محاولة حذف منتج مرتبط بطلبات سابقة → يُسمح بالحذف (Soft) لكن يبقى ظاهرًا في تفاصيل الطلبات القديمة بعلامة "منتج محذوف".
**النتيجة:** كتالوج محدَّث ودقيق.

---

### US-F3 — تنظيم الفئات والمجموعات
**الشروط المسبقة:** الأدمين مسجّل دخوله.
**التدفق الرئيسي:**
1. من "الفئات"، يضيف/يعدّل/يحذف فئة (اسم متعدد اللغات + صورة/أيقونة).
2. من "المجموعات"، ينشئ مجموعة ويربط بها عدة منتجات/فئات يدويًا (Drag & drop أو اختيار من قائمة).
**التدفقات البديلة:**
- حذف فئة تحتوي منتجات → تنبيه "هذه الفئة تحتوي X منتج، يجب نقلها لفئة أخرى أو حذفها أولًا" (منع الحذف المباشر لتفادي منتجات يتيمة).
**النتيجة:** هيكلة تصفح واضحة للزبائن.

---

### US-F4 — إدارة المحتوى المميز والبانرات
**الشروط المسبقة:** توجد منتجات/بانرات.
**التدفق الرئيسي:**
1. الأدمين يفعّل/يعطّل "مميز" على أي منتج (Toggle مباشر من قائمة المنتجات أو نموذج التعديل).
2. من قسم "بانرات الصفحة الرئيسية"، يرفع صورة بانر + رابط توجيه + ترتيب العرض + تاريخ بداية/نهاية اختياري.
**التدفقات البديلة:**
- بانر بلا تاريخ نهاية → يبقى نشطًا حتى يُعطَّل يدويًا.
**النتيجة:** صفحة رئيسية ديناميكية يتحكم بها الأدمين بالكامل.

---

### US-F5 — التسعير والعروض
**الشروط المسبقة:** منتج أو فئة موجودة.
**التدفق الرئيسي:**
1. الأدمين يعدّل السعر الأساسي مباشرة من نموذج المنتج.
2. لعرض/خصم: يفعّل "خصم" على منتج أو مجموعة منتجات (فئة كاملة)، يحدد نسبة أو قيمة ثابتة، وتاريخ بداية/نهاية العرض.
3. السعر بعد الخصم يُحسب ويُعرض تلقائيًا للزبائن (مع خط على السعر الأصلي).
**التدفقات البديلة:**
- انتهاء تاريخ العرض → يعود السعر تلقائيًا للسعر الأساسي دون تدخل يدوي (Job مجدول أو تحقق عند كل طلب عرض).
**النتيجة:** مرونة كاملة في استراتيجية التسعير.

---

## 🟤 Epic G — لوحة تحكم الأدمين: الطلبات

### US-G1 — قائمة كل الطلبات
**الشروط المسبقة:** الأدمين مسجّل دخوله.
**التدفق الرئيسي:**
1. الأدمين يفتح "الطلبات" → جدول بكل الطلبات، الأحدث أولًا، مع شارات ملوّنة للحالة.
2. عدّادات سريعة أعلى الصفحة (عدد الطلبات الجديدة اليوم، قيد الانتظار، إلخ).
**التدفقات البديلة:**
- طلبات كثيرة → Pagination + تحميل تدريجي.
**النتيجة:** رؤية شاملة وفورية لكل الطلبات الواردة.

---

### US-G2 — تفاصيل طلب
**التدفق الرئيسي:**
1. الأدمين يضغط على صف طلب → يفتح Drawer/Modal بالتفاصيل الكاملة: بيانات العميل (الاسم، الهاتف، الإيميل، الولاية)، قائمة المنتجات مع الكمية والسعر والإجمالي، تاريخ الطلب.
2. من نفس الشاشة: زر اتصال مباشر (`tel:`) وزر بريد مباشر (`mailto:`) لتسهيل التواصل.
**النتيجة:** كل ما يحتاجه الأدمين للتواصل مع العميل في مكان واحد.

---

### US-G3 — تغيير حالة الطلب
**التدفق الرئيسي:**
1. من تفاصيل الطلب، الأدمين يفتح Dropdown الحالة: `Pending → Confirmed → Completed`, أو `→ Rejected` في أي مرحلة.
2. يختار الحالة الجديدة → حفظ فوري + طابع زمني لكل تغيير حالة (Audit trail بسيط).
3. (اختياري حسب القرار النهائي) إرسال بريد تلقائي للعميل عند تغيير الحالة يُعلمه بالتحديث.
**التدفقات البديلة:**
- محاولة الرجوع من `Completed` إلى `Pending` → يُسمح لكن مع تحذير ("هذا الطلب مكتمل مسبقًا، هل أنت متأكد من التراجع؟").
**النتيجة:** حالة الطلب تعكس واقع المعالجة اليدوية بدقة.

---

### US-G4 — ملاحظات داخلية على الطلب
**التدفق الرئيسي:**
1. من تفاصيل الطلب، حقل نصي "ملاحظات داخلية" (غير مرئي للعميل إطلاقًا).
2. الأدمين يكتب ملاحظة ("تم الاتصال، العميل سيدفع عند الاستلام") ويضغط حفظ.
3. تُخزَّن الملاحظات كسجل (Array) بطابع زمني، وليس كحقل نصي واحد يُستبدَل، حتى تُبنى قصة تاريخية للتعامل مع الطلب.
**النتيجة:** ذاكرة مؤسسية لكل طلب، مفيدة عند تعدد التفاعلات.

---

### US-G5 — إشعار طلب جديد
**التدفق الرئيسي:**
1. فور إنشاء طلب جديد (US-D1)، يُنشأ إشعار داخل لوحة التحكم (Badge/عداد على أيقونة الجرس) + إشعار بريدي اختياري للأدمين.
2. الضغط على الإشعار يقود مباشرة لتفاصيل ذلك الطلب.
**النتيجة:** استجابة سريعة من الأدمين للطلبات الجديدة.

---

### US-G6 — بحث وفلترة الطلبات
**التدفق الرئيسي:**
1. من أعلى جدول الطلبات: حقل بحث (باسم العميل/رقم الطلب) + فلاتر (الحالة، الولاية، نطاق تاريخ).
2. كل تغيير يُحدّث الجدول فورًا مع حفظ الفلاتر في الـ URL.
**النتيجة:** إدارة فعالة حتى مع تراكم عدد كبير من الطلبات.

---

## ⚫ Epic H — لوحة تحكم الأدمين: الزبائن والتقييمات

### US-H1 — قائمة الزبائن
**التدفق الرئيسي:**
1. الأدمين يفتح "الزبائن" → جدول بكل الحسابات المسجَّلة (الاسم، البريد، الهاتف، تاريخ التسجيل، عدد الطلبات).
2. الضغط على زبون يفتح صفحته: بياناته الكاملة + سجل كل طلباته.
**النتيجة:** رؤية كاملة لقاعدة العملاء.

---

### US-H2 — حظر/تفعيل حساب
**التدفق الرئيسي:**
1. من صفحة الزبون، زر "حظر الحساب" → تأكيد → الحساب يصبح `isBanned: true`.
2. عند محاولة هذا الزبون تسجيل الدخول لاحقًا → رسالة "حسابك موقوف، تواصل مع الدعم" (كما في US-B2).
3. زر "إلغاء الحظر" يعيد الحساب لحالته الطبيعية.
**النتيجة:** تحكم كامل بالوصول للمنصة.

---

### US-H3 — مراجعة التقييمات
**التدفق الرئيسي:**
1. الأدمين يفتح "التقييمات" → قائمة التقييمات بحالة `PendingApproval` أولًا.
2. لكل تقييم: زر "قبول" (→ `Approved`، يظهر فورًا في صفحة المنتج) أو "رفض" (→ `Rejected`، لا يظهر أبدًا للعملاء لكن يبقى مخزنًا للسجل).
**النتيجة:** محتوى تقييمات موثوق ومُراقَب.

---

## ⚪ Epic I — لوحة تحكم الأدمين: التحليلات

### US-I1 — الإحصائيات العامة
**التدفق الرئيسي:**
1. الأدمين يفتح "لوحة التحليلات" (الصفحة الافتراضية بعد تسجيل الدخول).
2. تُجلب وتُحسب: إجمالي الطلبات (وبحسب الحالة)، عدد الزبائن الجدد (آخر 30 يومًا)، المنتجات الأكثر طلبًا (Top 5)، توزيع الطلبات حسب الولاية (رسم بياني/خريطة).
3. إمكانية تصفية الإحصائيات حسب نطاق تاريخ (هذا الأسبوع/الشهر/مخصص).
**التدفقات البديلة:**
- بيانات غير كافية (موقع جديد) → عرض حالة "لا توجد بيانات كافية بعد" بدل رسوم بيانية فارغة مربكة.
**النتيجة:** قرارات مبنية على بيانات حقيقية لتطوير الموقع والمخزون.

---

## ✅ تحديث الحالة العامة
- [x] الخطوة 1: أصحاب المصلحة
- [x] الخطوة 2: User Stories
- [x] الخطوة 3: وصف Flow تفصيلي لكل User Story (هذا الملف)
- [x] الخطوة 4: برومبت Google Stitch
- [x] الخطوة 5: برومبت OpenCode